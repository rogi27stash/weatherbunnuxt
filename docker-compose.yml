version: "3.8"

services:
  rogisu:
    
    ports:
      - "3003:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          memory: 128M